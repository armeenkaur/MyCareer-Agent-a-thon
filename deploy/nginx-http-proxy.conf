# Port 80 only — app behind nginx (no TLS). Fixes "Welcome to nginx".
# Use until system certs installed; then switch to nginx-https.conf.

upstream mycareer_app {
    server 127.0.0.1:5050;
    keepalive 32;
}

server {
    listen 80;
    server_name mycareercompass.mmt.com;

    location / {
        proxy_pass http://mycareer_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location /ws/ {
        proxy_pass http://mycareer_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}
